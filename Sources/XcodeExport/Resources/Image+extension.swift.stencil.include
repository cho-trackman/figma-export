{% for image in images %}
        {% if addObjcPrefix %}@objc {% endif %}static let {{ image.name }} = TdsLegacyIconResource(name: "{{ image.name }}"{% if not assetsInMainBundle %}, bundle: BundleProvider.bundle{% endif %}) {% endfor %}
